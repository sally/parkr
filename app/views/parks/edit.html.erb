Find me at views/parks/edit.html.erb

<div>
  Edit your park listing for <%= @park.name %>
</div>

<% if @errors %>
  U FUCKED UP
  <ul>
  <% @errors.each do |error| %>
    <li><%= error %></li>
  <% end %>
  </ul>
<% end %>

<div>
<%= form_for([@city, @park]) do |f| %>
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>

    <%= f.label :description %><br>
    <%= f.text_area :description %><br>

    <%= f.submit %>
  <% end %>
</div>